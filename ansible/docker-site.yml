---
- hosts: docker01
 become: yes
 remote_user: docker
 tasks:
     - name: deploy centos container
     docker: image=centos:centos6 name=test-container ports=80:80 expose=80 tty=yes
	  docker: image=centos:7 name=test-container privileged=yes  ports=7070:7070 expose=80 tty=yes command=/sbin/init
 
- hosts: test-container
 connection: docker
#   roles:
#     - { role: common, tags: 'common' }
#     - { role: nginx, tags: 'nginx' }
#     - { role: sshd, tags: 'sshd' }
   tasks:
     - include: roles/common/tasks/yum_update.yml
        tags: yum_update.yml
     - include: roles/common/tasks/yum_repos.yml
      tags: yum_repos.yml
     - include: roles/common/tasks/user.yml
       tags: common
     - include: roles/nginx/tasks/conf.yml
       tags: nginx-conf
